# 字节码、优化文件及动态链接库
__pycache__/
*.py[codz]
*$py.class

# C 扩展
*.so

# 分发与打包产物
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
# 通常在 PyInstaller 构建可执行文件之前，Python 脚本会根据模板生成这些文件，
# 以便向可执行文件中注入日期等信息。
*.manifest
*.spec

# 安装日志
pip-log.txt
pip-delete-this-directory.txt

# 单元测试与覆盖率报告
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# 翻译文件
*.mo
*.pot

# Django 相关文件：
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask 相关文件：
instance/
.webassets-cache

# Scrapy 相关文件：
.scrapy

# Sphinx 文档
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
# 对于需要在多种环境中运行的库或包，可以考虑忽略这些文件；
# 其他情况下可将其纳入版本控制：
# .python-version

# pipenv
# 根据 pypa/pipenv#598 的建议，应将 Pipfile.lock 纳入版本控制。
# 但在多人协作中，若依赖项仅适用于特定平台或不支持跨平台，
# pipenv 可能会安装无法正常工作的依赖，或无法安装全部所需依赖。
# Pipfile.lock

# UV
# 与 Pipfile.lock 类似，通常建议将 uv.lock 纳入版本控制。
# 对于二进制包，尤其建议保留锁文件以确保可复现性；库项目则更常选择忽略。
# uv.lock

# poetry
# 与 Pipfile.lock 类似，通常建议将 poetry.lock 纳入版本控制。
# 对于二进制包，尤其建议保留锁文件以确保可复现性；库项目则更常选择忽略。
# https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
# poetry.lock
# poetry.toml

# pdm
# 与 Pipfile.lock 类似，通常建议将 pdm.lock 纳入版本控制。
# pdm 建议保留 pdm.toml 中的项目级配置，但排除 .pdm-python。
# https://pdm-project.org/en/latest/usage/project/#working-with-version-control
# pdm.lock
# pdm.toml
.pdm-python
.pdm-build/

# pixi
# 与 Pipfile.lock 类似，通常建议将 pixi.lock 纳入版本控制。
# pixi.lock
# Pixi 会在 .pixi 目录中创建虚拟环境，类似于 venv 模块在 .venv 目录中创建虚拟环境。
# 建议不要将该目录纳入版本控制。
.pixi

# PEP 582；例如 github.com/David-OConnor/pyflow 和 github.com/pdm-project/pdm 会使用此目录
__pypackages__/

# Celery 相关文件
celerybeat-schedule
celerybeat.pid

# Redis
*.rdb
*.aof
*.pid

# RabbitMQ
mnesia/
rabbitmq/
rabbitmq-data/

# ActiveMQ
activemq-data/

# SageMath 解析生成的文件
*.sage.py

# 环境配置与虚拟环境
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder 项目设置
.spyderproject
.spyproject

# Rope 项目设置
.ropeproject

# mkdocs 文档
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre 类型检查器
.pyre/

# pytype 静态类型分析器
.pytype/

# Cython 调试符号
cython_debug/

# PyCharm
# JetBrains 专用模板由独立的 JetBrains.gitignore 文件维护，地址为：
# https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
# 可将其添加到全局 gitignore，或合并到本文件中。
# 也可取消下方规则的注释以忽略整个 .idea 目录，但通常不推荐这样做。
# .idea/

# Abstra
# Abstra 是一个由 AI 驱动的流程自动化框架。
# 忽略包含用户凭据、本地状态及设置的目录。
# 详情参见 https://abstra.io/docs
.abstra/

# Visual Studio Code
# Visual Studio Code 专用模板由独立的 VisualStudioCode.gitignore 文件维护，地址为：
# https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
# 可将其添加到全局 gitignore，或合并到本文件中。
# 如有需要，可取消下方规则的注释，以忽略整个 .vscode 目录。
# .vscode/
# 执行部分代码时生成的临时文件
tempCodeRunnerFile.py

# Ruff 相关文件：
.ruff_cache/

# PyPI 配置文件
.pypirc

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# Streamlit
.streamlit/secrets.toml

# PyCharm
.idea/

# AI Performance Lab 运行产物
runs/*
!runs/.gitkeep